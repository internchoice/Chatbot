{
  "name": "chatbot",
  "private": true,
  "engines": {
  "node": "16.x || 20.x"
},
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "test": "cd client && npm test"
    
  }
  
}
